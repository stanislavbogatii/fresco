export type ProductContent = {
    title: string;
    slug: string;
    description?: string;
    instructions?: string;
    storage_conditions?: string;
    ingredients?: string;
    langId: string;
}